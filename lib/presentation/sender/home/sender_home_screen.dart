import 'package:flutter/material.dart';

class SenderHomeScreen extends StatelessWidget {
  const SenderHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sender Home'),
    );
  }
}
