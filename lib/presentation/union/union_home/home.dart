import 'package:flutter/material.dart';

class UnionHomeScreen extends StatelessWidget {
  const UnionHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Union Home'),
    );
  }
}
