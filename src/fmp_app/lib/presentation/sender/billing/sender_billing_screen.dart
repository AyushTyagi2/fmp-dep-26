import 'package:flutter/material.dart';

class SenderBillingScreen extends StatelessWidget {
  const SenderBillingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('billing'),
    );
  }
}
