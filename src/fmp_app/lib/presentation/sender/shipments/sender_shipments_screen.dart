import 'package:flutter/material.dart';

class SenderShipmentsScreen extends StatelessWidget {
  const SenderShipmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Shipments'),
    );
  }
}
