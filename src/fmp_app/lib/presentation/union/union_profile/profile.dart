import 'package:flutter/material.dart';

class UnionProfileScreen extends StatelessWidget {
  const UnionProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Profile'),
    );
  }
}
