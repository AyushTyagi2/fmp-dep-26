import 'package:flutter/material.dart';

class UnionQueueScreen extends StatelessWidget {
  const UnionQueueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Queue'),
    );
  }
}
