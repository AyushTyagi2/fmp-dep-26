import 'package:flutter/material.dart';

class UnionRequestScreen extends StatelessWidget {
  const UnionRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My Requests'),
    );
  }
}
